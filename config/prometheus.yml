global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "node_exporter"
    static_configs:
      - targets:
          - "host.docker.internal:9100"

  - job_name: "tracker_app"
    static_configs:
      - targets:
          - "tracker:8000"
